# ~/.tmuxinator/theiconic.yml

name: theiconic
root: ~/

windows:
  - console: cd && clear
  - dashboard: cd ~/repos/tablero && yarn start
  - solr: solr restart -f
  - shop:
      layout: even-horizontal
      panes:
          - cd ~/repos_theiconic/alice && clear
          - cd ~/repos_theiconic/bob && clear
  #- assets: composer/bin/phing -f dev.xml global-build
  #- alice.log: tail -f alice/tmp/borges-debug.log
  #- bob.log: tail -f bob/data/logs/borges-debug.log
